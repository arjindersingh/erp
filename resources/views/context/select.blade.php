<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Select context</title>@vite(['resources/css/app.css'])</head><body class="min-h-screen bg-slate-100"><main class="mx-auto max-w-2xl px-6 py-12"><h1 class="text-3xl font-bold">Choose your working context</h1><p class="mt-2 text-slate-600">{{ $tenant->name }}</p>@if($errors->any())<div role="alert" class="mt-5 rounded-xl bg-red-50 p-4 text-red-800">{{ $errors->first() }}</div>@endif<form method="post" action="{{ route('context.store') }}" class="mt-8 space-y-5 rounded-2xl bg-white p-7 shadow">@csrf
<label class="block font-semibold">Membership<select class="mt-1 w-full rounded-xl border-slate-300" name="membership" required>@foreach($memberships as $membership)<option value="{{ $membership->uuid }}">{{ $membership->accessScope->name }} — {{ $membership->membership_type->value }}</option>@endforeach</select></label>
<label class="block font-semibold">Portal<select class="mt-1 w-full rounded-xl border-slate-300" name="portal" required>@foreach($portals as $portal)<option value="{{ $portal->code }}">{{ $portal->name }}</option>@endforeach</select></label>
<label class="block font-semibold">Academic year<select class="mt-1 w-full rounded-xl border-slate-300" name="academic_year" required>@foreach($years as $year)<option value="{{ $year->uuid }}">{{ $year->name }}</option>@endforeach</select></label><button class="rounded-xl bg-cyan-800 px-5 py-3 font-bold text-white">Continue</button></form></main></body></html>
